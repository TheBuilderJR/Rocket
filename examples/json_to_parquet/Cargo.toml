[package]
name = "json_to_parquet"
version = "0.1.0"
edition = "2021"

[dependencies]
rocket = "0.5"
serde = { version = "1.0", features = ["derive"] }
serde_json = "1.0"
parquet = "2.0"
datafusion = "6.0" # Optional: Only if SQL querying over parquet files is implemented
